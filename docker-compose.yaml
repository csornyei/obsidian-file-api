services:
  file_api:
    image: file-api-image:latest
    container_name: file_api_container
    ports:
      - "8080:8080"
    environment:
      - FILE_API_ENV=production
      - FILE_API_DEBUG=false
    volumes:
      - ./data:/app/data
    restart: unless-stopped
